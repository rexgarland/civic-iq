import { Question } from "./Question.js";
import { renderMistake } from "./render.js";

describe("html rendering functions", () => {
  describe("renderMistake", () => {
    it("should show all the options of the original question for context", () => {
      // const text = "Approximately half of federal taxes are spent on:";
      // const options = ["military", "transportation", "wellfare"];
      // const answer = "wellfare";
      // const question = new Question(text, options, answer);

      // const html = renderMistake({ choice: "military", question });

      // expect(html).toContain("transportation");
    });
  });
});
